[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"
  NITRO_PRESET = "netlify"

# Catch-all: send everything through the Netlify Edge/Function handler Nitro generates
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200

import { n as useAuth } from "./useAuth-45kOB3fg.js";
import { Navigate, useLocation } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Loader2 } from "lucide-react";
//#region src/components/auth/AuthGuard.tsx
function AuthGuard({ children, requireAssessment = false, requireAdmin = false }) {
	const { session, profile, isAdmin, loading } = useAuth();
	const location = useLocation();
	if (loading) return /* @__PURE__ */ jsxs("div", {
		className: "flex min-h-screen flex-col items-center justify-center bg-background p-4",
		children: [/* @__PURE__ */ jsx(Loader2, { className: "size-8 animate-spin text-primary" }), /* @__PURE__ */ jsx("p", {
			className: "mt-3 text-sm text-muted-foreground",
			children: "Verifying authentication..."
		})]
	});
	if (!session) return /* @__PURE__ */ jsx(Navigate, {
		to: "/login",
		search: { redirect: location.href },
		replace: true
	});
	if (requireAdmin && !isAdmin) return /* @__PURE__ */ jsx(Navigate, {
		to: "/patient/dashboard",
		replace: true
	});
	if (requireAssessment && !profile?.assessment_completed) return /* @__PURE__ */ jsx(Navigate, {
		to: "/assessment",
		replace: true
	});
	return /* @__PURE__ */ jsx(Fragment, { children });
}
//#endregion
export { AuthGuard as t };
